import { test, expect } from '@playwright/test';

test.describe("Test Suite for Log in tests", ()=> {

    test.describe("Test Suite for Log in tests with email address", ()=> {
        test('Log in with valid email address', async ({ page }) => {
 
        });
        test('Log in with invalid email address', async ({ page }) => {
 
        });
        test('Log in with empty email address field', async ({ page }) => {
 
        });
    })

    test.describe("Test Suite for Log in tests with username", ()=> {
        test('Log in with valid username', async ({ page }) => {
 
        });
        test('Log in with invalid username', async ({ page }) => {
 
        });
        test('Log in with empty username field', async ({ page }) => {
 
        });
    })
});